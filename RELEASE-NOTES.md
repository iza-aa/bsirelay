# Release Notes — BSI Relay

## 🚀 v1.2.0 (Latest)
- **Multi-Language Architecture Expansion:**
  - Added TypeScript Interactive CLI suite (`npx bsirelay` with `setup`, `doctor`, `status`, `sync`, `inspect`).
  - Added Python Computer Vision & Visual Analyzer (`tools/figma_analyzer.py` for token classification & canvas measurement).
  - Added Python Rule Distillation Engine (`tools/distill_rules.py`).
- **Multi-Agent Native Manifests:**
  - Added native manifest support for 15+ AI coding agents: Claude Code, Antigravity, Codex, Cursor, Devin, Hermes, Kimi, OpenCode, Pi, Gemini.
- **Decoupled Architecture:**
  - Separated private `pilar-docs` companion repository with graceful on-demand synchronization.
  - 100% sanitized Git history.
- **Fast Approval Gate:**
  - Standardized on `"go"` keyword with aliases (`run`, `proceed`, `yes`).

## 📦 v1.1.0
- Added `/setup` onboarding skill with automatic `chrome-devtools-mcp` installation.
- Added 3-Layer Decoupled Architecture (Framework, Team, Personal).
- Standardized `/learn` feedback interrupt skill.

## 📦 v1.0.0
- Initial release of BSI Relay framework.
