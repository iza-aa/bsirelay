# Installing bsirelay in OpenCode

To install `bsirelay` in OpenCode, clone the repository into your project's `.agents` directory:

```bash
git clone https://github.com/iza-aa/bsirelay.git .agents
```

Then tell OpenCode:
```text
Load instructions and memory from .agents/teammemory.md and follow the skills defined in .agents/skills/
```
